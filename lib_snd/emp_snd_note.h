//-----------------------------------------------------------------------------
// rgen.art
//-----------------------------------------------------------------------------
#ifndef EMP_SND_NOTE_H
#define EMP_SND_NOTE_H

//-----------------------------------------------------------------------------
//-----------------------------------------------------------------------------
#include "emp_tim_time.h"

//-----------------------------------------------------------------------------
//-----------------------------------------------------------------------------
namespace emp { namespace snd {

//-----------------------------------------------------------------------------
//-----------------------------------------------------------------------------
// @@0  : EMP_CLASS
class note_c
{
public:
    ui32 index;
    ui64 time;
    ui64 duration;

    note_c(void):
        EMP_TT_MAX_CTOR(index),
        EMP_TT_MAX_CTOR(time),
        EMP_TT_MAX_CTOR(duration)
    {}

    note_c(
        c_ui32 a_uiIndex,
        c_ui64 a_uiTime,
        c_ui64 a_uiDuration):
        index(a_uiIndex),
        time(a_uiTime),
        duration(a_uiDuration)
    {}

    EMP_RETURN bool operator==(note_c const& a_rFrom) const
    {
        return
            index == a_rFrom.index &&
            time == a_rFrom.time &&
            duration == a_rFrom.duration;
    }

    EMP_RETURN bool operator!=(note_c const& a_rFrom) const
    { return operator==(a_rFrom) == false; }
};

//-----------------------------------------------------------------------------
//-----------------------------------------------------------------------------
} }

//-----------------------------------------------------------------------------
//-----------------------------------------------------------------------------
#endif

